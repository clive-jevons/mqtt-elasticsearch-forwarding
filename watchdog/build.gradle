apply plugin: "groovy"
apply plugin: "application"

mainClassName = "Watchdog"

repositories {
  mavenCentral()
}

dependencies {
  compile group: 'org.codehaus.groovy', name: 'groovy-all', version: '2.4.7'
}
