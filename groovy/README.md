# Groovy MQTT / ES forwarder

The application is built using Gradle. To build, change into the `consumer`
sub-dir and execute `gradle build`. After this, build the docker image
by executing `./docker_build`.
